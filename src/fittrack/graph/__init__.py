"""The LangGraph orchestration (spec 8).

Empty for now; `GraphState` and the root graph land with the sprint that builds
them. `test_graph_reducers` and `test_graph_topology` deliberately arrive in
that same PR rather than here — a topology test over no topology would pass
without proving anything, which is worse than no test (sprint S01-T07, step 3).
"""
