"""One module per agent: prompt, schema and runner (spec 9).

Empty for now. Like `graph/`, the package exists so the architecture guardrail
has something to enforce before there is anything to enforce it against.
"""
